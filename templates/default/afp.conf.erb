;
; Netatalk 3.x configuration file
;

[Global]
<% if node['time_machine']['spotlight'] %>
spotlight = yes
<% end %>

<% if node['time_machine']['homes']['enabled'] %>
[Homes]
basedir regex = <%= node['time_machine']['homes']['regex'] %>
<% end %>

<% @volumes.each do |volume| %>
<% volume = render_config(volume) %>
<%= volume.to_s %>
<% end %>
